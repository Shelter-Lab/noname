electron_mirror="https://npmmirror.com/mirrors/electron/"
# 本仓库各子包用独立 lockfile(sharedWorkspaceLockfile: false),根 lockfile 不含 mobile 等子包依赖。
# CI 默认 frozen-lockfile=true 会因此误判"lockfile 不一致"而失败,故关闭。
frozen-lockfile=false